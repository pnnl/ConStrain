# ConStrain Streamlit UI - Archived

This document has been archived.

Current status:
- Legacy fallback/reference only
- Not part of the supported API-first docker-compose runtime
- Replaced by `api-server` and `api-ui` in `docker/docker-compose.yml`

Archived copy:
- See `docker/container_docs/archive/STREAMLIT_DOCKER.md`